# 개발일지

---

## 2026-06-27 ~ 06-28 · PlayScene UI 리팩토링

### 작업 개요

`PlayScene_Widget.cpp`의 UI 레이아웃을 비율 기반(`Renderer::GetResolution().x * 0.xx`) 좌표에서 픽셀 절대좌표로 전면 교체했습니다.
해상도가 1024×600으로 고정되어 있어 절대좌표가 오히려 배치 의도를 명확하게 표현합니다.

---

### 변경 내용

#### PlayScene_Widget.cpp

**1. 레이아웃 상수화**
```
const int BTN_RADIUS = 8;   // 버튼 모서리 반경 통일
```

**2. 상단 바 (y=8, h=40)**

| 요소 | 이전 | 이후 |
|---|---|---|
| 결과화면으로 가기 | ratio (0.1x, 0.1y) w=160 | x=8, y=8, w=120×40 |
| 남은 턴 라벨 | ratio (0.35x, 0.15y) w=150 | x=388, y=8, 140×40 |
| 턴 카운트 | ratio (0.5x, 0.15y) w=60 | x=534, y=8, 66×40 |
| 다음 턴 | ratio (0.85x, 0.4y) | x=870, y=8, 146×40 |

**3. 유닛 소환 버튼 (y=510, h=52, w=116, 간격 14)**

| 버튼 | 이전 | 이후 |
|---|---|---|
| 정착자 소환 | ratio (0.4x, 0.8y) | x=324 |
| 전사 소환 | ratio (0.6x, 0.8y) | x=454 |
| 궁수 소환 | ratio (0.8x, 0.8y) | x=584 |

세 버튼 모두 `(1024 - 116×3 - 14×2) / 2 = 324`을 기점으로 정확히 정렬했습니다.

**4. 정보 패널 (x=8, y=456, w=210, h=110)**

- 배경색: `(180, 180, 13)` 황색 → `(48, 50, 113)` 다크블루
- 모서리 반경: `10` → `BTN_RADIUS`
- 타일 이름 텍스트: `FVector2(x - w/2, y - h*2/3)` → `FVector2(14, 462)` / `196×28`
- 행동 횟수 텍스트: `FVector2(x - w/2, y - h/3)` → `FVector2(14, 494)` / `196×28`
- 턴 스킵 버튼: ratio → cx=69, cy=544, `110×36`

**5. 팝업 (중앙 x=342, y=272, w=340, h=56)**

- 크기: `320×60` → `340×56`
- 배경색: 황색 → 다크블루
- 모서리 반경: `10` → `BTN_RADIUS`

**6. 기타 정리**
- `WidgetComponents`에 `m_spawnSettelerUnitButtonText` 중복 등록 제거
- 파일 끝 파일 스코프에 남아있던 `int sh = 36;` 단독 선언 제거
- `ref->SetContent(std::to_wstring(10.0f))` 디버그 잔재 제거

---

### 트러블슈팅 · 실행 환경 이슈

게임 실행 시 `ACharacter::LoadAnimationData`에서 Access Violation (0xc0000005) 크래시 발생.

**원인 분석**
- `FFileHelper`가 리소스 경로를 `"../Resource/%s/%s"` 상대경로로 구성
- 실행 디렉토리(CWD)가 솔루션 루트(`C:\ChangJinGithub\GDIProject`)로 설정되면 `../Resource/` 해석 실패
- `infos` 벡터가 비어있는 상태에서 `infos[0].size()` 호출 → 크래시

**해결**
- CWD를 `GDIProject\x64\`로 설정하면 `../Resource/` → `GDIProject\Resource\`로 올바르게 해석
- 정상 실행 확인 및 플레이씬 전체 UI 렌더링 스크린샷 확보

---

### 현재 상태

- 코드 수정 완료, 리빌드 및 최종 시각 검증은 다음 세션에서 진행 예정

